import { getPictureSet } from '../../../../utilities/utilities'
import map from './map.jpg'
import map_s from './map_s.jpg'

const images = {
	map: getPictureSet(map),
	map_s: getPictureSet(map_s)
}

export default images